# Copiar a .env.local. La app REQUIERE estas dos variables (sin ellas muestra
# "no hay datos"). Lee public.monitor_snapshot con el cliente de Supabase (anon
# key, solo lectura vía RLS) — mismo patrón que app-tareas. Son valores públicos
# y seguros; en Vercel se ponen como Environment Variables.
NEXT_PUBLIC_SUPABASE_URL=https://<ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>

# Solo servidor (NO NEXT_PUBLIC, no se exponen al browser): las usa /api/refresh
# cuando el usuario aprieta "Actualizar", para forzar un recálculo pegándole al
# Lambda. LAMBDA_API_KEY = la misma PUBLISHER_API_KEY del Lambda.
LAMBDA_URL=https://<id>.lambda-url.<region>.on.aws/
LAMBDA_API_KEY=<publisher-api-key>
